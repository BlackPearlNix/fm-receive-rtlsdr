The receiver is RTL-SDR dongle. It can be tuned to FM frequency using SDR block.  
The IQ samples will be baseband FM ( the down conversion happens in side the dongle). FM broadcast channels allocated 200 kHz. So we will process that much amount of information.  
The demodulation flow has two methods: inbuilt block ad custom procedure. 
